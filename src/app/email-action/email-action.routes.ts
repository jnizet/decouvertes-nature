import { EmailActionComponent } from './email-action/email-action.component';
import { Routes } from '@angular/router';

export const EMAIL_ACTION_ROUTES: Routes = [
  {
    path: '',
    component: EmailActionComponent
  }
];
